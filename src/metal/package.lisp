;;;; src/metal/package.lisp -- CRT.METAL is defined in util/package.lisp.
;;;;
;;;; All packages live in one file so the layering is visible in one place; this
;;;; file exists only to give the module a first component that establishes the
;;;; package for the readers that follow.

(in-package #:cathode-ray-tube.metal)
